const ContactButton = () => {
  return (
    <button
      type="submit"
      className="py-3 px-5 text-sm font-medium text-center text-white rounded-lg bg-[#3649F9] sm:w-fit hover:brightness-90 focus:ring-4 focus:outline-none focus:ring-[#3649F9] transition-all cursor-pointer"
    >
      Kirim Pesan
    </button>
  );
};

export default ContactButton;